#include "main.h"

/*_strlen - finds the length of a string entered by a
 * user
 * @input: user input
 * Return: the string length
 */

int _strlen(char *input)
{
	int i;

	for(i = 0; input[ii] != '\0'; i++)
	{
		;
	}
	return (i);
}
